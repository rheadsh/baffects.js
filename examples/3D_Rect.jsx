#include "../lib/baffects.js"function setup() {    b.cleanComp();        b.set3DMode(b.ENABLE);    var shape = b.rect(b.width/2, b.height/2, 0, 1000, 1000);    b.expression(shape.orientation, "wiggle(0.2, 360)");    b.motionBlur(shape);        var light = b.light();    b.expression(light.color, "wiggle(1, 1)");        var cam = b.camera();    b.expression(cam.position, "wiggle(1, 1000)");         b.set3DMode(b.DISSABLE);    b.stroke(1, 1, 0);    b.strokeWeight(10);     b.line(0, b.height/2, b.width, b.height/2);}b.go("Quad", 2048, 2048, 10);